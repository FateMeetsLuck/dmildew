/*
The Math library is fully implemented and has all of the same functions as standard JavaScript Math.
*/
function getRandomNumber(min, max) {
    return min + Math.floor(Math.random() * (max-min-1));
}

console.log("Getting 10 random numbers between 1 and 10");
for(let i = 0; i < 10; i += 1) {
    console.log(getRandomNumber(1, 10));
}
// As of Feb 18 15:53 2021 This program appears to produce the expected output