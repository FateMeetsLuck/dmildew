// This program is more optimized if one uses a direct constant instead of assigning one
let start = Date.now();
for(let i=0; i < 1000000; ++i) {

}
let totalms = Date.now() - start;
console.log(`It took ${totalms} ms to count to 1000000`);
// As of Feb 18 15:51 2021 it takes ~1430 ms to execute the for loop in a debug build and ~1276 ms in the release build
