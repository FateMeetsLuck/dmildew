const bytes = new Uint8Array([0, 0, 0, 1]);
const raw = new ArrayBuffer(bytes);
const float = new Float32Array(raw);
console.log(bytes);
console.log(float[0]);


const data = Uint8Array.from("test");
console.log(String.fromCodePoint(Array.from(data)));

