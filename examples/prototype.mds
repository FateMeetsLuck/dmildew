function Person(name, age) {
    this.name = name;
    this.age = age;
}

Person.prototype.building = "Brick house";
Person.prototype.greet = function() {
    testPrint(this.name + " says hello!");
};

var p1 = new Person("Santa", 79);

testPrint(p1);
